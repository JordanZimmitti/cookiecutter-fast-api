# File for adding or overwriting docker dev environment variables found in
# {{cookiecutter.package_name}}/core/settings.py

# The type of environment the api is running in
ENVIRONMENT=dev

# Fast-Auth API server database host
API_DB_HOST=postgresql